# Database Configuration
DATABASE_URL=postgresql://rust_user:rust_password@localhost:5432/rust_api_db

# Server Configuration
SERVER_HOST=127.0.0.1
SERVER_PORT=8080

# JWT Configuration
JWT_SECRET=sua-chave-secreta-jwt-mude-em-producao-deve-ser-longa-e-segura
JWT_EXPIRATION=3600

# Rate Limiting Configuration
RATE_LIMIT_RPM=60
RATE_LIMIT_BURST=10

# Docker PostgreSQL Configuration (for reference)
# If using Docker Compose, the following values are set automatically:
# POSTGRES_DB=rust_api_db
# POSTGRES_USER=rust_user
# POSTGRES_PASSWORD=rust_password
# POSTGRES_PORT=5432

# PgAdmin Configuration (accessible at http://localhost:8081)
# Email: admin@example.com
# Password: admin123

# Default Admin User (created automatically)
# Email: admin@sistema.com
# Password: admin123 (CHANGE IMMEDIATELY AFTER FIRST LOGIN!)
